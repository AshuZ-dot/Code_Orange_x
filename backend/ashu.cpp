//write code here
#include<bits/stdc++.h>
using namespace std;

int main(){
   string s,t;
   cin >> s;
   cin >> t;
   int ans=s.find(t);
   if(ans>=0 && ans<s.size()){
     cout << ans << endl;
   }
   else{
     cout << ans << endl;
   }
}